import {
  render
} from "@testing-library/react";

import { RandomLetterChosenListener } from "../../../../../screens/board/listeners/RandomLetterChosenListener/RandomLetterChosenListener";

describe("Test RandomLetterChosenListener", () => {
  it("Call back is execute when letter is chosen", () => {
    // render(<RandomLetterChosenListener />);
  });
});
